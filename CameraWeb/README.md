CameraWeb is a basic web interface that allows for testing the HQ camera interface and adafruitmh driver without using any componenets of Indigo-astronomy.

cd to the directory where you place these files and run "camera.py". 

It will tell you the IP address and port to use in your browser. The arrow buttons move the focuser while clicking the image will take a new picture.

